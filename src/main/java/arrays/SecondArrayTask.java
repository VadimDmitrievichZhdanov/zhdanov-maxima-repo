package arrays;

public class SecondArrayTask {
    public static int fullSize(int[] firstArray, int[] secondArray) {
        return firstArray.length + secondArray.length;
        //самый тупой метод в истории Java
    }
}
